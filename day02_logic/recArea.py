# Finding the area of a rec A = w*l

# int would only take whole numbers
# float would handle both int/float
width = float(input("What is the Width: "))
length = float(input("What is the length: "))

area = width * length

print (f"The area of your rec is {area} cm^2.")

